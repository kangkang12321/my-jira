# Jira Clone 启动指南

## 推荐方案：使用 SQLite（无需 Docker）

由于 Docker 权限问题，推荐使用 SQLite 版本快速启动。

### 🚀 一键启动

```bash
# 运行启动脚本
./start-sqlite.sh
```

### 📝 手动启动

```bash
# 1. 安装依赖（首次运行）
cd frontend && npm install
cd ../backend && npm install

# 2. 启动后端（SQLite）
cd backend
npm run dev:sqlite

# 3. 在新终端启动前端
cd frontend
npm run dev
```

### ✨ 访问地址
- 前端：http://localhost:3000
- 后端：http://localhost:3001

---

## 方案 B：使用 MySQL（需要 Docker）

### 1. 修复 Docker Desktop

打开 Docker Desktop，确保：
- 菜单栏图标显示 "Docker Desktop is running"
- 没有错误提示

### 2. 启动数据库

```bash
# 启动 MySQL
docker-compose up -d

# 等待 30 秒让数据库初始化
sleep 30

# 检查状态
docker ps
```

### 3. 启动后端

```bash
cd backend
npm run start:dev
```

### 4. 启动前端

```bash
cd frontend
npm run dev
```

---

## 📂 数据库文件位置

- **SQLite**: `backend/data/jira_clone.db`
- **MySQL**: Docker volume `mysql_data`

---

## 🔄 切换数据库

### 从 SQLite 切换到 MySQL

```bash
# 1. 确保数据库已启动
docker-compose up -d

# 2. 后端使用 MySQL 配置
cd backend
npm run start:dev
```

### 从 MySQL 切换到 SQLite

```bash
cd backend
npm run dev:sqlite
```

---

## ❓ 常见问题

### Q: 后端启动失败？
```bash
# 检查数据库
cd backend
npm run test:db

# 如果失败，使用 SQLite
npm run dev:sqlite
```

### Q: 前端无法连接后端？
确保后端在 3001 端口运行：
```bash
curl http://localhost:3001
```

### Q: 想要重置数据库？
```bash
# SQLite
rm backend/data/jira_clone.db

# MySQL
docker-compose down -v
docker-compose up -d
```

---

## 🎯 推荐开发流程

使用 SQLite 进行日常开发：
```bash
./start-sqlite.sh
```

部署到生产环境时再切换到 MySQL。